{
  "name": "yellow-river-media-intake",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "黄河影像征集核验入库服务",
  "scripts": {
    "test": "node --test",
    "build": "for f in src/*.js; do node --check \"$f\" || exit 1; done"
  }
}
